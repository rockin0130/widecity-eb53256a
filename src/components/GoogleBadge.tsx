import { Chrome } from "lucide-react";

export default function GoogleBadge() {
  return (
    <div className="flex items-center justify-center w-5 h-5 rounded-full bg-white border text-black">
      <Chrome size={12} />
    </div>
  );
}
